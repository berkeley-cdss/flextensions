name: Cucumber Tests

on:
  push:
  pull_request:

jobs:
  cucumber:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
        ports:
          - 5432:5432
        options: --health-cmd="pg_isready" --health-interval=10s --health-timeout=5s --health-retries=3
    env:
      # Do not install production/staging-only gems in test environment
      BUNDLE_WITHOUT: "production staging"
      DB_HOST: localhost
      DB_USER: postgres
      DB_PASSWORD: password
      CANVAS_URL: http://bcourses.test.instructure.com/
      CHROME_DATA_DIR: /tmp/chrome-data-cucumber
      COVERAGE: "true"
      SELENIUM_HEADLESS: "true"
      SELENIUM_DRIVER_VERSION: stable

    steps:
    - name: Install packages
      run: sudo apt-get update && sudo apt-get install --no-install-recommends -y google-chrome-stable
    - uses: actions/checkout@v4
    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: .tool-versions
        bundler-cache: true
    - name: Set up Node
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: npm
    - name: Install npm packages
      run: npm ci --omit=dev
    - name: Install Chrome and ChromeDriver
      uses: browser-actions/setup-chrome@v1
      with:
        chrome-version: stable
    - name: Setup Database
      run: bundle exec rails db:setup
    - name: Build CSS
      run: bundle exec rails dartsass:build
    - name: Create Chrome data directory
      run: mkdir -p /tmp/chrome-data-cucumber
    - name: Run non-JavaScript Cucumber tests
      env:
        COVERAGE_COMMAND: cucumber-rack_test
      run: |
        bundle exec cucumber features/ --profile rack_test
    - name: Run JavaScript Cucumber tests
      # Run even if the previous step failed so both suites contribute to the
      # merged coverage report (the job still fails if any step fails).
      if: always()
      env:
        COVERAGE_COMMAND: cucumber-javascript
      run: |
        DISPLAY=:99
        Xvfb :99 -screen 0 1920x1080x24 > /dev/null 2>&1 &
        bundle exec cucumber features/ --profile javascript
    - name: Report merged coverage
      if: always()
      run: ruby .github/scripts/coverage_summary.rb "Cucumber coverage (rack_test + javascript)"
